// tests/setup/global-teardown.ts
// Jest global teardown: clean up resources after all tests

export default async () => {
  // Example: close in-memory DB, clear mocks, etc.
  // You can add more teardown logic here if needed
}; 